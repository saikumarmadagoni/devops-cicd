pipeline {
    agent { label 'master' }
    stages {
        stage('test') {
          steps {
              def pipeline = load pipeline.groovy
              pipeline.checkout()
              pipeline.sonarscan()
          }
        }

    }
}
